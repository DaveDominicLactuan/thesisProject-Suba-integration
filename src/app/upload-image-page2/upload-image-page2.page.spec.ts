import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UploadImagePage2Page } from './upload-image-page2.page';

describe('UploadImagePage2Page', () => {
  let component: UploadImagePage2Page;
  let fixture: ComponentFixture<UploadImagePage2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UploadImagePage2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
