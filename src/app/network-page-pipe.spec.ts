import { NetworkPagePipe } from './network-page-pipe';

describe('NetworkPagePipe', () => {
  it('create an instance', () => {
    const pipe = new NetworkPagePipe();
    expect(pipe).toBeTruthy();
  });
});
