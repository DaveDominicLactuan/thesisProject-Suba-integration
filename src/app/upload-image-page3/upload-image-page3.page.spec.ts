import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UploadImagePage3Page } from './upload-image-page3.page';

describe('UploadImagePage3Page', () => {
  let component: UploadImagePage3Page;
  let fixture: ComponentFixture<UploadImagePage3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UploadImagePage3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
